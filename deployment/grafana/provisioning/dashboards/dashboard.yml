apiVersion: 1

providers:
  - name: 'tcp-udp-router'
    orgId: 1
    folder: 'TCP/UDP Router'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards